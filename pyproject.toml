[project]
name = "plugin_manager"
version = "0.0.1"
description = "Toggle other plugins on and off"
authors = [{name = "bb6-Bgofish"}]
dependencies = []

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.4.2"
required_features = []